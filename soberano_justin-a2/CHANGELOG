September 13:
    - Added a struct for the world.
    - Saves the position of all of the gates. 

September 15
    - Added n, s, e, w generation.
    - Fixed a bug where new terrain would override old terrain.
    - Paths now connect to new generated maps.
    - Paths are now generated based on surrounding neighbors if already generated.
    - Added 'f x y' fly to generation.
    - Added Pokemart and Pokecenter generation percentage to decrease linearly the farther the map is generated by using the Manhattan distance.

September 16
    - Changed file name from 'generate_terrain.c' to 'world.c'
    - Created Makefile
    - Added gate prevention on world borders.